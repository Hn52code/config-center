server:
  port: 9050
spring:
  application:
    name: config-client

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
  instance:
    prefer-ip-address: true
#    instance-id: micro-provider